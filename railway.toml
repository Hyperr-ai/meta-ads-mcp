[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -m meta_ads_mcp --transport streamable-http --host 0.0.0.0 --port 8080"
healthcheckPath = "/mcp/v1/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
